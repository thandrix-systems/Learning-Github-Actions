apiVersion: v1
kind: Service
metadata:
  name: thandrix-springboot-service
  labels:
    app: thandrix-app
spec:
    type: NodePort
    selector:
        app: thandrix-app
    ports:
        - protocol: TCP
          port: 8000
          targetPort: 8000
          nodePort: 30080        